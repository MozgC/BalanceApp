<Project>
<PropertyGroup>
	<TargetFramework>net10.0</TargetFramework>  <!-- applies to all projects unless overridden -->
	<LangVersion>latest</LangVersion>
	<Nullable>enable</Nullable>
	<!-- other common settings -->
</PropertyGroup>
</Project>